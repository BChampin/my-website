# champin.dev
## v0.1

Run the website with `docker compose up`, access it on [http://localhost:10000](http://localhost:10000).

**Status :** working :check_mark_button:

:warning: You will not have the database nor the .env file, but website should be mostly up and running, as needed for this archive objective.
