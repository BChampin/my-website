services:
  website-v3-php-mvc:
    image: php:7.4.3-apache
    container_name: website-v3-php-mvc
    restart: unless-stopped
    # Only expose port if direct access is needed
    # ports:
    #   - 10003:80
    volumes:
      - ./:/var/www/html
    networks:
      - my-website-network
