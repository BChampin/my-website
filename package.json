{
  "name": "my-website",
  "version": "0.0.0",
  "private": true,
  "description": "Common trunk for champin.dev and its sub-sites (apps/*), sharing a Vue + Tailwind component layer (packages/ui)",
  "type": "module",
  "scripts": {
    "dev": "vp run -F './apps/*' dev",
    "build": "vp run -F './apps/*' build",
    "pages:build": "pnpm build && node scripts/assemble-pages.mjs",
    "check": "vp check",
    "lint": "vp lint",
    "fmt": "vp fmt"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.30.3"
}
