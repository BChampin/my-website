services:
  website-v5-php:
    image: php:7.4.3-apache
    container_name: website-v5-php
    restart: unless-stopped
    # Only expose port if direct access is needed
    # ports:
    #   - 10005:80
    volumes:
      - ./:/var/www/html
    networks:
      - my-website-network
